before_script:
  - sudo apt-get update -qq && sudo apt-get upgrade -y
  - sudo apt-get install -y -qq build-essential gawk unzip libncurses-dev libz-dev libssl-dev wget git subversion python-pip python3-pip

fullbuild:
  script:
    - make V=s FORCE_UNSAFE_CONFIGURE=1 GLUON_BRANCH=experimental GLUON_RELEASE=v2018.2.1 GLUON_GIT_REF=12b87b244180cf10051da4c86007305e0185faf3
